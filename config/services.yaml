# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:
    shopware.store.frw: '1'
    default_cdn_strategy: 'physical_filename'
    shopware.cdn.strategy: '%env(default:default_cdn_strategy:SHOPWARE_CDN_STRATEGY_DEFAULT)%'
    env(OPENSEARCH_URL): 'http://localhost:9200'

services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.
        # Binding arguments by name or type
        # https://symfony.com/doc/current/service_container.html#binding-arguments-by-name-or-type
        #bind:
        #    'bool $isDebug': '%kernel.debug%'

    App\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/Entity/'
            - '../src/Kernel.php'

    # XAMPP on Windows reports generated PEM files as 666 even after chmod().
    # Disable the POSIX key permission check for local Windows development.
    shopware.private_key:
        class: League\OAuth2\Server\CryptKey
        arguments:
            - '%shopware.api.jwt_key.private_key_path%'
            - '%shopware.api.jwt_key.private_key_passphrase%'
            - false

    shopware.public_key:
        class: League\OAuth2\Server\CryptKey
        arguments:
            - '%shopware.api.jwt_key.public_key_path%'
            - null
            - false
